[agents]
max_threads = 6   # tope duro de Codex; 4 dejaba 1/3 de la capacidad sin usar
max_depth = 1
job_max_runtime_seconds = 600   # timeout por worker en spawn_agents_on_csv (default 1800 es excesivo para un lote)
